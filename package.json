{
  "name": "balloon-site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "yarn build && scp -r ./dist/* martiiian@5.101.50.67:~/repos/erika/tour-site"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0"
  }
}
